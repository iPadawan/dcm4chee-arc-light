import { ConvertBytePipe } from './convert-byte.pipe';

describe('ConvertBytePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertBytePipe();
    expect(pipe).toBeTruthy();
  });
});
